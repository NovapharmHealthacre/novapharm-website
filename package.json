{
  "name": "novapharm-healthcare-website",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Enterprise-grade NovaPharm Healthcare website with secure portal, admin surfaces, SEO/GEO optimization, and SharePoint integration architecture.",
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "start": "node server.mjs",
    "start:production": "npm run build && node scripts/sync-secure-content.mjs && node server.mjs",
    "validate": "node scripts/validate-site.mjs && node scripts/check-links.mjs && node scripts/validate-app.mjs && node scripts/validate-domain.mjs",
    "validate:site": "node scripts/validate-site.mjs",
    "validate:app": "node scripts/validate-app.mjs",
    "validate:domain": "node scripts/validate-domain.mjs",
    "check:links": "node scripts/check-links.mjs",
    "test": "node scripts/test-server.mjs",
    "check": "npm run build && npm run validate && npm test",
    "hash:password": "node scripts/generate-password-hash.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
